function repeatStringNumTimes(str, num) {
 if (num < 0)
return ""
 if (num > 0)
  return str.repeat(num) 
}

repeatStringNumTimes("abc", 3);

//quick script that repeats a given string 
//free code camp challange